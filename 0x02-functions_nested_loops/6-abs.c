#include"main.h"

/**
 * _abs- computes absolute value
 * @n: number to be computed
 *
 * Return: Absolute value of @n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (n * -1);
	}
}
